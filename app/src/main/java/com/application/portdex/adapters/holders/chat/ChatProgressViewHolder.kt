package com.application.portdex.adapters.holders.chat

import com.application.portdex.databinding.ProgressListViewBinding


class ChatProgressViewHolder(val view: ProgressListViewBinding) : ViewHolder<Any>(view.root) {
    override fun onBind(data: Any) {
    }
}